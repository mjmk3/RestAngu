spring:
  datasource:
    url: jdbc:mysql://localhost/restu
    username: root
    password:

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: ture
    database-platform: org.hibernate.dialect.MySQLDialect
    open-in-view: false

  session:
    jdbc:
      initialize-schema: always